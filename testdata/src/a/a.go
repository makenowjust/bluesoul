package a

// the the main function // want "duplicate article is found"
func main() {
	// a a variable // want "duplicate article is found"
	var x string
	// an an integer value // want "duplicate article is found"
	const i = 42

	println(x, i)
}
